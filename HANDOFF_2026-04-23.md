# HANDOFF — 2026-04-23

## Session scope
Initialize ae8-app project governance. Port the portable PBX–Claude
constitution from the Consensus Protocol LLC Project, layer
project-specific instructions on top.

## Delivered
- `CONSTITUTION.md` — redacted copy of portable constitution. Infra
  secrets (relay endpoint / token) intentionally omitted because the
  repo is public. Full version lives in Project knowledge base only.
- `PROJECT_INSTRUCTIONS.md` — ae8-app-specific layer: architecture,
  brand, deployment, project-specific rules, project-specific bans,
  open work list.
- `.gitignore` — minimal sane defaults (OS junk, editor state, secrets).
- This handoff log.

## NOT delivered this session
- No code changes to `index.html`, `sw.js`, `manifest.json`, or assets.
- No commits pushed to origin. Commit is staged locally in the sandbox
  only — pushing requires credentials PBX holds.
- No `LAWS.md` created. Per constitution Part 7, LAWS is optional; the
  few project-specific rules fit cleanly in PROJECT_INSTRUCTIONS §5–6.

## Open questions raised this session
1. **4090 usage mismatch.** PBX's opening request said "utilize our
   4090s to collab on this project." Constitution Appendix A says "4090s
   don't help a static site — don't invent a use for them." Proceeded
   on the static-PWA assumption. If a GPU-backed feature (ML recs, film
   analysis, coach agent) is actually planned, needs scoping as a
   separate phase.
2. **Knowledge base persistence.** Constitution was uploaded in chat but
   not yet added to the Project knowledge base via Settings. Without
   that step, future chats in this Project will not have it available
   automatically — this was the exact issue that opened the session.
3. **Deploy URL.** GitHub Pages not yet confirmed enabled. Need to
   verify `pjice.github.io/ae8-app/` is live (or will be) before any
   public share.

## Next-session queue (priority order)
1. PBX adds `CONSTITUTION.md` + `PROJECT_INSTRUCTIONS.md` to this
   Project's knowledge base (Settings → Add content).
2. Push this session's commit to origin.
3. Enable GitHub Pages; verify the live URL renders.
4. Address missing icon assets (`icon-192.png`, `icon-512.png`
   referenced by `manifest.json`).
5. Prune or date-gate stale camp cards.
6. Scope the audio licensing decision before any public launch.

## Evidence
*(Filled in after commit — see commit message on `main` for hashes and
regression result.)*
